1. Install Cuda for GPU (pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118)
2. Install Whisper Local (pip install -U openai-whisper)
3. Install ffmpeg (choco install ffmpeg, sudo apt update && sudo apt install ffmpeg)